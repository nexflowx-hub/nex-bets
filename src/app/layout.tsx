import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "NeX Bets — O Epicentro do Mundial 2026",
  description:
    "Plataforma premium de apostas desportivas para a FIFA World Cup 2026. Odds em tempo real, mercados expandidos e experiência Hacker-Punk.",
  keywords: ["apostas", "World Cup 2026", "FIFA", "odds", "betting", "futebol"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
